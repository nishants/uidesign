-------------------------

-------------------------
{
  "body": {
    "search" : "{{request.query.search}}",
    "page"   : "{{request.query.page}}",
    "size"   : "{{request.query.size}}"
  }
}
-------------------------
-------------------------